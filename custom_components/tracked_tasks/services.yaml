mark_done:
  name: Mark done
  description: Compatibility action for marking a tracked task as completed. Prefer button.press targeting button.<task>_mark_done.
  target:
    entity:
      domain: button
      integration: tracked_tasks
  fields:
    task_id:
      name: Task ID (legacy)
      description: Legacy YAML task identifier to mark as completed. Prefer targeting the task's mark-done button entity.
      required: false
      example: bins
      selector:
        text: {}
