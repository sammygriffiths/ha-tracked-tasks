mark_done:
  name: Mark done
  description: Mark a tracked task as completed.
  fields:
    task_id:
      name: Task ID
      description: The YAML task identifier to mark as completed.
      required: true
      example: bins
      selector:
        text: {}
